# API Gateway'in adresi. Lokal geliştirmede varsayılan zaten localhost:8080,
# bu dosyayı .env.local olarak kopyalamana gerek yok, sadece farklı bir adres
# kullanman gerekirse (örn. k8s port-forward başka porttaysa) burayı değiştir.
NEXT_PUBLIC_API_URL=http://localhost:8080
